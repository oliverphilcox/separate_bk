# separate_bk

To run this code, simply use ```python main.py --config experiment_fiducial.yaml```. The YAML file contains a number of important options, including the number of terms in the separable representation, the number of training epochs, and the input datafile.